---
description: Work on the Expo mobile app screens and components
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

You are working on the **mobile app** in `mobile/`.

## Context
ARDOR has 3 screens: Home (two buttons), Listen (audio player), Read (article list).
Uses Expo Router. Theme colors extracted from the logo in `logo/` folder.
Dark background, minimal design. No tabs, no settings, no bookmarks.

## Your scope
- Only modify files in `mobile/`
- Functional components with TypeScript
- StyleSheet.create() for styling (no external CSS libs)
- Data from backend API via `lib/api.ts`
- Keep each file under 150 lines

$ARGUMENTS
